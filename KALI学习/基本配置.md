```bash
apt-get update & apt-get upgrade
更新软件与列表
```

```bash
apt-get dselect -upgrade
#安装下载更新
```

```bash
apt-get install liunx-headers-$(uname -r)
#linux内核更新
apt autoremove#卸载不需要的软件
dpkg -i sogoupinyin_2.2.0.0108_amd64.deb
#dpkg -i安装指定的deb安装包
apt-get install -f
#解决依赖包问题

```

